# NPM Configuration for Deployment
# Handle peer dependency conflicts
legacy-peer-deps=true
auto-install-peers=true

# Disable husky hooks during installation
prepare=false
ignore-scripts=true

# Use npm instead of pnpm for consistency
package-manager=npm

# Performance optimizations
registry=https://registry.npmjs.org/
prefer-offline=false